{
  "name": "JobOps Template - Interviews Notify",
  "nodes": [
    {"parameters":{"workflowInputs":{"values":[{"name":"chatId"}]}},"id":"1ed0ae0e-3253-4305-b4a7-f4167efa0e9a","name":"Called by Router","type":"n8n-nodes-base.executeWorkflowTrigger","typeVersion":1.2,"position":[0,0]},
    {"parameters":{"operation":"get","dataTableId":{"__rl":true,"mode":"id","value":"YOUR_JOBOPS_TEMPLATE_CONFIG_DATA_TABLE_ID"},"limit":1},"id":"8a7bfc97-af64-47d3-ad5b-eb8c8501382d","name":"Load Config","type":"n8n-nodes-base.dataTable","typeVersion":1.1,"position":[224,0]},
    {"parameters":{"chatId":"=YOUR_TELEGRAM_CHAT_ID","text":"={{ \"New interview? Log it directly here: https://airtable.com/\" + $json.baseId + \"/\" + $json.tableInterviews }}","additionalFields":{"appendAttribution":false}},"id":"ca2df91b-de3f-4e12-b653-9befacdbf53a","name":"Send Interviews Link","type":"n8n-nodes-base.telegram","typeVersion":1.2,"position":[448,0],"webhookId":"0cff3c89-c974-468c-a63d-5ac869b71860"},
    {"parameters":{"content":"## Part of the JobOps Template\n\nIn live use (triggered via the router by a real Telegram message), chatId is supplied automatically (no setup needed).\n\n**If you test/execute this workflow standalone** (the \"Called by Router\" trigger, not through a live message), there's nothing upstream to populate chatId; you must enter a real chat ID yourself as test input, or this workflow can't send anything. \nFind yours: message this template's bot once → open **JobOps Template Bot Router** → click the **JobOps Template Bot Trigger** node → **Listen for test event** → send another message → read `message.chat.id` from the captured output.\n\nSee **JobOps Template - Application Reminders** / **JobOps Template - Reviews Digest** for the two workflows that always need a real chat ID hardcoded.","height":432,"width":524,"color":"#DC8ADD"},"id":"4a9d0e86-d8b6-40de-bf8b-7a9b6c5373dc","name":"Setup Note - Chat ID","type":"n8n-nodes-base.stickyNote","typeVersion":1,"position":[16,-464]},
    {"parameters":{"content":"## Setup required: jobops_template_config Data Table ID (always, no exceptions)\n\nThis workflow looks up the Airtable base/table IDs it needs from an n8n Data Table (D-24) rather than hardcoding them or using n8n Variables (gated behind a paid plan). The placeholder `YOUR_JOBOPS_TEMPLATE_CONFIG_DATA_TABLE_ID` in the **Load Config** node MUST be replaced with your own Data Table's ID before this workflow can do anything, in every run, not just for testing.\n\n**How to set it up:**\n1. Create a Data Table in your n8n instance (Data Tables → New), name it `jobops_template_config`.\n2. Add one row with a column for every base/table ID your own Airtable base uses (e.g. baseId, tableInterviews).\n3. Open the **Load Config** node in this workflow → click into the Data Table field → select your new table. This replaces the placeholder with your table's real ID.\n\n**Important:** this workflow is exported with the placeholder still in place. It will fail (harmlessly) until you fill in your real Data Table ID. After editing it, you must **republish this workflow** for the change to take effect — editing alone only updates the draft, not what's actually running.","height":416,"width":900,"color":"#DC8ADD"},"id":"b2c3d4e5-6f7a-48e9-9f1b-2c3d4e5f6a7b","name":"Setup Note - Data Table ID","type":"n8n-nodes-base.stickyNote","typeVersion":1,"position":[16,-936]}
  ],
  "connections": {
    "Called by Router": {"main":[[{"node":"Load Config","type":"main","index":0}]]},
    "Load Config": {"main":[[{"node":"Send Interviews Link","type":"main","index":0}]]}
  },
  "settings": {"executionOrder":"v1","availableInMCP":true,"binaryMode":"separate"}
}
